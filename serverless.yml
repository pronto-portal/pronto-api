service: pronto-api
provider:
  name: aws
  runtime: nodejs14.x
  region: us-east-2
functions:
  index:
    handler: dist/index.handler
    events:
      - http:
          path: graphql
          method: post
          cors: true
      - http:
          path: graphql
          method: get
          cors: true
plugins:
  - serverless-offline
custom:
  webpack:
    includeModules: true
  serverless-offline:
    httpPort: 4000
